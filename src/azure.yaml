# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: MvcMusicStore
metadata:
  template: MvcMusicStore@1.0.0

services:
  web:
    project: ./MvcMusicStore
    language: dotnet
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ./
      remoteBuild: true
